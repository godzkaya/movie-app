pipeline {
    agent any

    stages {

        stage('test') {
            steps {
                echo 'test stage'
            }
        }

        stage('build') {
            steps {
                echo 'just dev'
            }
        }
    }
}
